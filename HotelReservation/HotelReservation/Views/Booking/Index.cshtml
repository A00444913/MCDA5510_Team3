@TempData["x"]


@{
    ViewData["Title"] = "Booking Room";
}
<style>
    body {
        align-content: center;
        align-items: center;
        text-align: center;
    }
</style>

<div class="findroom">
    <div class="container-fluid">
    <img src="~/images/logo.png" width="200" />
    <h1>@ViewData["Title"]</h1>



    <form class="form-group" asp-controller="Booking" asp-action="SearchRoom" method="post">
    <label class="col-2">From Date</label>
    <input name="From" type="date" class="col-3"/>
    <p>to</p>
    <input name="To" type="date" style="width:400px" class="end_date" />
    <br />

    <p style="font-size:25px">Location</p>
    <select name="city" style="width:400px">
        <option value="Toronto">Toronto</option>
        <option value="New York">New york</option>
        <option value="Halifax">Halifax</option>
    </select>

    <p style="font-size:25px">Rooms</p>
    <input name="room" type="number" style="width:400px" class="room_number" placeholder="Number of rooms" min="1" />
    <p style="font-size:25px">Guests</p>
    <input name="guest" type="number" style="width:400px" class="guest_number" placeholder="Number of guests" min="1" />

    <p >type of room</p>
        <select name="type" >
            <option value="king">king size</option>
            <option value ="Queen">queen size</option>
        </select> 
        <input type="submit"  style="width:400px;color:white;background-color:rebeccapurple; font-size:20px"/>
    </form>
    <div class="container p-3">
        <div class="row pt-4">
            <div class="col-6">
                <h2 class="text-primary">Hotel list</h2>
            </div>
            <div class="col-6">
                Create New Hotel
            </div>
        </div>
        <br /><br /><br />
        @*if (Model.Count() > 0)
        {
            <table class="table table-bordered table-striped" style="width:100%">
                <thead>
                    <tr>
                        <th>
                            Hotel Id
                        </th>
                        <th>
                            Hotel Location
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var obj in Model)
                    {
                        <tr>
                            <td width="50%">@obj.HotelId</td>
                            <td width="30%">@obj.Location</td>
                            <td></td>
                        </tr>
                    }
                </tbody>
            </table>

        }
            else
            {
                <p>No Hotel exists.</p>
            }*@
        </div>
    </div>
</div>
